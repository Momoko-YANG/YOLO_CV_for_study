# Frontend

这是手势识别系统的 Web 前端，基于 React、TypeScript 和 Vite。

## 开发

```bash
npm install
npm run dev
```

默认通过 Vite 代理把：

- `/api` 转发到 `http://localhost:8000`
- `/ws` 转发到 `ws://localhost:8000`

## 构建

```bash
npm run build
```
